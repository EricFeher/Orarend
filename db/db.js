/*const mysql = require('mysql2/promise');

const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'orarend'
});

/*db.connect(err => {
    if(err){
        throw err;
    } else{
        console.log("PeePeePooPoo check");
    }
});*/
//	module.exports = db;